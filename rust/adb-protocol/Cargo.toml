[package]
name = "adb-protocol"
version = "0.1.0"
edition = "2021"

[dependencies]
adb-types = { path = "../adb-types" }
zerocopy = { version = "0.8.38", features = ["derive"] }
thiserror = "1.0"

[lints]
workspace = true
