# ADB Client-Server Test

This directory contains a test harness for testing the ADB client-server protocol.
This is the so-called "smart protocol" in the ADB documentation, representing the communication between the ADB client (the CLI) and the ADB server listening on port 5037.